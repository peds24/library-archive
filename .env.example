# Google Books API key — used as secondary fallback after Open Library.
# Get one free at: https://console.cloud.google.com → APIs & Services → Credentials
# Enable "Books API" in the library first, then create an API key.
# Without this key, the fallback is skipped and manual entry is shown instead.
EXPO_PUBLIC_GOOGLE_BOOKS_API_KEY=
